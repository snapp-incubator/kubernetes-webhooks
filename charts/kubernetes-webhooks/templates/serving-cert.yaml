apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kubernetes-webhooks.fullname" . }}-serving-cert
  labels:
  {{- include "kubernetes-webhooks.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "kubernetes-webhooks.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc'
  - '{{ include "kubernetes-webhooks.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc.{{ .Values.kubernetesClusterDomain }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "kubernetes-webhooks.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert
